#!/usr/bin/env bash

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(find ~/caiomcg_vault -mindepth 1 -maxdepth 4 -type f | fzf)
fi

if [[ -z $selected ]]; then
    exit 0
fi

nvim $selected
